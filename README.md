# config.nvim

My neovim config.

## installation

If you have a pre-existing config at [~/.config/nvim]() rename it to something
like `nvim.old`.

```bash
cd ~/.config && git clone git@github.com:anton-oz/config.nvim
```
